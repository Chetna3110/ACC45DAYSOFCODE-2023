#include <stdio.h>
int main(void) {
	int T;
	scanf("%d",&T);
	while(T--)	{
	    int x,n;
	    scanf("%d%d",&n,&x);
	    if(x>n-x)
	    printf("%d\n",n-x);
	    else
	    printf("%d\n",x);
	}
	return 0;
}
